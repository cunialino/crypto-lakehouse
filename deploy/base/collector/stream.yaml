apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: tradesstream
spec:
  name: tradesstream
  subjects: ["exchange.*"]
  storage: file
  maxAge: 72h
  replicas: 3
---
apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: gapstream
spec:
  name: gapstream
  subjects: ["trade.*"]
  storage: file
  maxAge: 72h
  replicas: 3
